# Development environment variables
# Set the base path for the application (default is root "/")
VITE_BASE_PATH=/

# To deploy under a subpath like /ui/, uncomment the line below:
# VITE_BASE_PATH=/ui/

# API endpoint prefix (if different from base path)
# VITE_API_BASE_URL=
